{
  "name": "apresentacao-produto",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build --out dist --without-notes",
    "export:pdf": "slidev export --format pdf",
    "export:pptx": "slidev export --format pptx"
  },
  "devDependencies": {
    "@slidev/cli": "^52.15.2",
    "@slidev/theme-default": "^0.25.0"
  }
}
